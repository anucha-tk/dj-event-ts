import React from "react";

import Layout from "@/components/Layout";

const Add = () => {
  return (
    <Layout title={"Add New Event"}>
      <h1>add</h1>
    </Layout>
  );
};

export default Add;
